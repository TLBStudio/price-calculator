<!DOCTYPE html>
<html lang="en" class="h-full">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Professional web and software project estimation tool for agencies and freelancers">
        <title>{% block title %}TLB Pricing - Project Estimator{% endblock %}</title>

        <!-- Favicon -->
        <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>💰</text></svg>">

        <!-- Preconnect to external resources -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <style>
                /* Custom scrollbar */
                ::-webkit-scrollbar {
                    width: 8px;
                }

                ::-webkit-scrollbar-track {
                    background: #f1f5f9;
                    border-radius: 4px;
                }

                ::-webkit-scrollbar-thumb {
                    background: #cbd5e1;
                    border-radius: 4px;
                }

                ::-webkit-scrollbar-thumb:hover {
                    background: #94a3b8;
                }

                /* Smooth transitions */
                * {
                    transition: all 0.2s ease-in-out;
                }

                /* Dialog element styling */
                dialog {
                    border: none;
                    border-radius: 1rem;
                    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
                    padding: 0;
                    margin: auto;
                    max-width: 90vw;
                    max-height: 90vh;
                }

                dialog::backdrop {
                    background: rgba(0, 0, 0, 0.6);
                    backdrop-filter: blur(8px);
                }

                /* Ensure dialog is responsive */
                @media (max-width: 640px) {
                    dialog {
                        margin: 1rem;
                        max-width: calc(100vw - 2rem);
                        max-height: calc(100vh - 2rem);
                    }
                }

                /* Custom focus styles */
                .form-focus:focus {
                    outline: none;
                    ring: 2px;
                    ring-color: #3b82f6;
                    ring-offset: 2px;
                }

                /* Loading animation */
                @keyframes pulse {
                    0%, 100% { opacity: 1; }
                    50% { opacity: 0.5; }
                }

                .animate-pulse {
                    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="h-full bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 min-h-screen">
        {% block body %}{% endblock %}
    </body>
</html>
